<h1>Characters Found</h1>
{{#if heroData.length}}
<h2>{{ searchTerm }}</h2>
<ul>
    {{#each heroData}}  
    <li>
    <a href="/characters/{{id}}">{{name}}</a>
    </li>
    {{/each}}
</ul>
{{else}}
<p class="not-found">We're sorry, but no results were found for {{ searchTerm }}.</p>
{{/if}}
<a href="/"><br>Back to Search</a>
